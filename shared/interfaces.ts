export interface IMeta {
  title: string;
  description: string;
}

export interface ICharacterInfo {
  id: string;
  name: string;
  firstName?: string;
  lastName?: string;
  faceIcon: string;
  element: string;
}
